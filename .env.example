# Optional: Remote GraphQL endpoint. Defaults to a public schema.
GRAPHQL_ENDPOINT=https://countries.trevorblades.com/graphql

# Optional: Authorization header value for protected endpoints.
# Example: Bearer your-token
GRAPHQL_AUTH_HEADER=
